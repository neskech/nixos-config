# Development Libraries Configuration
# System libraries and dependencies for development
{ config, pkgs, ... }:

{
  # Development libraries will be added here as needed
  # Example: environment.systemPackages = with pkgs; [ libffi openssl ];
  
}
